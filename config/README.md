# config/

Reserved for later-phase configuration that isn't pure environment
variables — e.g. RBAC role definitions (Phase 9), Fast-Path Router rules
(Phase 3), and LLM-as-Judge scoring rubrics (Phase 10).

Empty in Phase 0 by design.
